# Copy to .env and fill in real values. Used by docker-compose.yml via
# `env_file: .env` - every td2hive CLI option can also come from an
# identically-named environment variable (see td2hive/cli.py).
TD_HOST=
TD_USER=
TD_PASSWORD=

OBS_ACCESS_KEY=
OBS_SECRET_KEY=
OBS_ENDPOINT=
OBS_BUCKET=
